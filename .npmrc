# @neondatabase/auth is in beta and its peer deps (lucide-react >=0.469,
# an optional next>=16 hint) don't line up with this project's pinned
# versions yet. legacy-peer-deps keeps installs (local, CI, Cloudflare
# Pages) working without needing a manual --legacy-peer-deps flag.
legacy-peer-deps=true
